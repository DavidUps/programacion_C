#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){

    char chess[4][10];
    char resul[4];
    char check[2];

    printcheck(chess);
    printresul(resul);
    isnertresul(resul);
    for(int i=0;i>11;i++){
        for(i>11){
            insetchess(chess);
            printcheck(chess);
        }while(false);
    }

    return EXIT_SUCCESS;
}
