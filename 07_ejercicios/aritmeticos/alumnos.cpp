#include <stdio.h>
#include <stdlib.h>

int main(){
    int lista, fila, num=17;

    printf("Numero: ");
    scanf(" %i\n", &lista);

    do{
        lista /num= fila;
    }

    while(lista > 17);

    printf("%i", fila);

    return EXIT_SUCCESS;
}
